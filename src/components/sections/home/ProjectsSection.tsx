const ProjectsSection = () => {
  return (
    <section className="h-screen w-full bg-background flex items-center justify-center border-b border-primary">
      <h1 className="text-9xl font-heading font-black uppercase text-primary">Projects Section</h1>
    </section>
  );
};

export default ProjectsSection;
